{
    "name": "Fake-Mail Gen BOT",
    "description": "Fake Mail gen bot",
    "repository": "https://github.com/InukaRanmira/Temp-Mail-Bot",
    "keywords": [
        "Bot",
        "Telegram ",
        "Pyrogram "
    ],
    "env": {
        "API_ID": {
            "description": "Get from my.telegram.org/apps",
            "required": true
        },
        "API_HASH": {
            "description": "Get from my.telegram.org/apps",
            "required": true
        },
        "BOT_TOKEN": {
            "description": "get from @BotFather",
            "required": true
        },
        "DATABASE": {
            "description": "get from mongo",
            "required": true
        },
        "BOT_USERNAME": {
            "description": "Your Bot Username without @",
            "required": true
        },
        "LOG_GROUP": "Your Log Group ID With -",
        "required" : true
    },
    "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  },
    "buildpacks": [
        {
            "url": "heroku/python"
        }
    ]
}
